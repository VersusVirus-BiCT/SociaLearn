import { ProfileIconDirective } from './profile-icon.directive';

describe('ProfileIconDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileIconDirective();
    expect(directive).toBeTruthy();
  });
});
